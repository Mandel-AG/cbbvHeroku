<% admins.forEach((admin) => {%>
    <ul>
            <li id='adminId' class='list-group-item active'><%= admin.id %></li> 
            <li class='list-group-item'><%= admin.email %></li> 
            <button class="btn btn-danger " id="delete" adminid= <%- admin._id %> >Delete</button>
    </ul>
    <% }) %>